# Las variables
uribe <- "es un paraco" # una variable de caracter
fpositivo <- 10000 # una variable numerica
bondad_uribe <- FALSE # una variable logica
gobierno <- 9i # una variable compleja

# Los vectores
hijos_uribe <- c("idiota", "perro", "satan en persona", "novia de Duque")
soldados <- c(1, 2, 3, 4, 5, 6, 7, 8, 9)
batallon <- c(1:1000)
batallon <- c(1000:1)

# Vector
batallon[(batallon %% 2) == 0]
